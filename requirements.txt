# Python 3.11
google-genai>=1.0          # API Gemini image (types.ImageConfig : aspect_ratio, image_size)
pillow>=10.0               # assemblage, vignettes, seuillage
numpy>=1.26
opencv-python-headless>=4.9  # Canny, dilatation (QA)
vtracer>=0.6               # vectorisation ; repli possible sur le binaire potrace
pytest>=8.0                # tests

# Blender en module Python (~370 Mo). Sans GPU il faut Mesa en logiciel :
#   apt-get install libegl1 libgl1-mesa-dri libgles2 libxi6 libxxf86vm1 libxfixes3 \
#                   libxrender1 libsm6 libxkbcommon0 libgomp1
#   export LIBGL_ALWAYS_SOFTWARE=1
# et pip fige numpy<2 : opencv 5 s'en plaint mais fonctionne.
bpy>=4.2
